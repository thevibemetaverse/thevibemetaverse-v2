# Anthropic (prompt → voxel codegen)
CLAUDE_API_KEY=

# Portals service base URL (origin only). Server proxies /portals.json → ${PORTALS_SERVER}/portals.json
# Default: http://localhost:3001
# PORTALS_SERVER=https://portals.yourdomain.com
