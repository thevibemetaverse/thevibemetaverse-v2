# Anthropic (prompt → voxel codegen)
CLAUDE_API_KEY=

# Optional: portals origin for /portals.json proxy. Omit in production (defaults to the live portals Railway app).
# PORTALS_SERVER=http://localhost:3001
